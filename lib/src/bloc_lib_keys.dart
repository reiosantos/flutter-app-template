import 'package:flutter/widgets.dart';

mixin BlocLibraryKeys {
  // Holds keys shared across the bloc app
  static const Key extraActionsPopupMenuButton =
      Key('__extraActionsPopupMenuButton__');
}
